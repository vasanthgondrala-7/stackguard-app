
  # User Authentication Flow

  This is a code bundle for User Authentication Flow. The original project is available at https://www.figma.com/design/wRQHHVpDtYUtAtjIDxd93O/User-Authentication-Flow.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  